import React from 'react';
import { TextField, Button, Typography } from '@material-ui/core';

//Following the template add textfield, buttons and props with typography

const Form = () => {
  return (
    <h1>FORM</h1>
  )
}

export default Form;
